import React from 'react'
import styles from './Loader.module.css'

const Loader = () => (
	<div className={styles.ripple} data-testid="loader">
		<div>
			</div><div>
		</div>
	</div>
)

export default Loader